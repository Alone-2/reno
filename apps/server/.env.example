# ── 数据库 ──
DATABASE_URL=postgresql://USER:PASSWORD@127.0.0.1:5432/reno

# ── JWT ──
JWT_SECRET=change-me-to-a-random-64-char-string
JWT_ACCESS_TTL=2h
JWT_REFRESH_TTL=7d

# ── 注册开关 ──
ENABLE_REGISTRATION=false

# ── 服务 ──
PORT=3000

# ── 文件上传 ──
MAX_FILE_SIZE=20971520
