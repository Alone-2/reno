{
  "name": "reno",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build",
    "check": "vp check",
    "test": "vp run -r test",
    "build": "vp run -r build",
    "dev": "vp run web#dev",
    "dev:server": "vp run server#dev",
    "build:server": "vp run server#build",
    "prepare": "vp config"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.9.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
